[mysqld]
bind-address            = 0.0.0.0
default_storage_engine  = InnoDB

[client]
default-character-set   = utf8mb4

[mysql]
default-character-set   = utf8mb4

[mysqldump]
default-character-set   = utf8mb4
